[project]
name = "alpha-analysis-downstream-processing-mcp"
version = "0.1.0"
description = "MCP server for Alpha analysis downstream processing and automation"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "boto3>=1.42.28",
    "google-api-python-client>=2.188.0",
    "google-auth>=2.47.0",
    "google-auth-httplib2>=0.3.0",
    "google-auth-oauthlib>=1.2.2",
    "mcp>=1.14.1",
    "openai>=2.15.0",
    "pydantic>=2.12.5",
    "pydantic-settings>=2.12.0",
    "python-dotenv>=1.1.1",
    "pymupdf>=1.25.0",
    "requests>=2.32.5",
]

[project.scripts]
alpha-analysis-downstream-processing-mcp = "alpha_analysis_downstream_processing_mcp.server:main"

[tool.ruff]
line-length = 100
target-version = "py313"

[tool.ruff.lint]
select = [
    "E",    # pycodestyle errors
    "W",    # pycodestyle warnings
    "F",    # pyflakes
    "I",    # isort
    "B",    # flake8-bugbear
    "C4",   # flake8-comprehensions
    "UP",   # pyupgrade
]
ignore = [
    "E501",  # line too long, handled by black
    "B008",  # do not perform function calls in argument defaults
]

[tool.ruff.format]
quote-style = "double"
indent-style = "space"

[tool.mypy]
python_version = "3.13"
warn_return_any = true
warn_unused_configs = true
disallow_untyped_defs = true
disallow_incomplete_defs = true
check_untyped_defs = true
disallow_untyped_decorators = true
no_implicit_optional = true
warn_redundant_casts = true
warn_unused_ignores = true
warn_no_return = true
warn_unreachable = true

[dependency-groups]
dev = [
    "mypy>=1.18.2",
    "pytest>=8.0",
    "ruff>=0.13.1",
    "types-boto3>=1.42.28",
    "types-requests>=2.32.4.20260107",
]

[tool.uv]
package = true
